//
//  ProductCardView.swift
//  DemoOffLineDBApp
//
//

import SwiftUI


/*struct ProductCardView: View {
    let product: Product
    let onFavorite: () -> Void

    var body: some View {
        VStack(alignment: .leading) {
            Text(product.title).font(.headline)
            Text("₹ \(product.price)")

            Button(product.isFavorite ? "❤️" : "🤍") {
                onFavorite()
            }
        }
        .padding()
        .background(.gray.opacity(0.1))
        .cornerRadius(12)
    }
}
*/
